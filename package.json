{
  "name": "persona-paths",
  "version": "0.1.34",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build backend.ts --outfile backend.js --target bun && bun build frontend.ts --outfile frontend.js --target browser",
    "typecheck": "tsc --noEmit",
    "test": "node tests/persona-selection.mjs && node tests/model-picker.mjs"
  },
  "devDependencies": {
    "lumiverse-spindle-types": "latest",
    "typescript": "^7.0.2"
  }
}
